const $=(q)=>document.querySelector(q);const store={get:(k,d)=>JSON.parse(localStorage.getItem(k)||JSON.stringify(d)),set:(k,v)=>localStorage.setItem(k,JSON.stringify(v))};function tick(){document.querySelectorAll('[data-clock]').forEach(e=>e.textContent=new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}));document.querySelectorAll('[data-date]').forEach(e=>e.textContent=new Date().toLocaleDateString([], {weekday:'long',month:'long',day:'numeric'}))}setInterval(tick,1000);addEventListener('DOMContentLoaded',()=>{tick();setTimeout(()=>$('.splash')?.classList.add('hide'),900)});function renderNotes(){const n=store.get('notes',[]),list=$('#notesList');if(!list)return;list.innerHTML=n.map((x,i)=>`<div class="item"><b>${x.title}</b><p class="muted">${x.body}</p><button class="button" onclick="delNote(${i})">Delete</button></div>`).join('')||'<p class="muted">No notes yet.</p>'}function addNote(){let title=$('#noteTitle').value||'Untitled',body=$('#noteBody').value||'';store.set('notes',[{title,body},...store.get('notes',[])]);$('#noteTitle').value='';$('#noteBody').value='';renderNotes()}function delNote(i){let n=store.get('notes',[]);n.splice(i,1);store.set('notes',n);renderNotes()}function calc(v){try{$('#calcDisplay').value=Function(`return (${v})`)()}catch{$('#calcDisplay').value='Error'}}function saveSetting(){store.set('settings',{name:$('#owner').value,theme:$('#theme').value});alert('Settings saved')}function loadSettings(){const s=store.get('settings',{name:'Rohan',theme:'Nebula'});if($('#owner')){$('#owner').value=s.name;$('#theme').value=s.theme}}
